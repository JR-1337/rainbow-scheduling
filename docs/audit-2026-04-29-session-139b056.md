# /audit session — s040 sweep — 2026-04-29

## Verdict
**Needs Attention** — Correctness regression in `MobileScheduleGrid` (titled employees see role-name in cell where blank intended), plus an L gap in mobile admin day-cell keyboard nav that the s040 sweep missed.

## Health trend (vs. prior audit)
- FIRST_RUN for slug `session-139b056`. Prior session-mode report covered `session-40cf842` (cdc07f1). Most prior findings shipped in s039+s040.

## Fixed autonomously (this audit)
None — bucket-1 + bucket-2 deferred into a single plan per JR direction.

## Deferred — ranked

### Correctness bugs (I)
- **C1** `src/MobileEmployeeView.jsx:301` — `isTitled: false` hardcoded inside `MobileScheduleGrid` cell render. `MobileAdminView.jsx:366` correctly computes `hasTitle(emp)`. Result: titled admins (e.g. Sarvi) viewed in employee-mobile grid show role-name where blank intended. `hasTitle` already imported at line 24.
- **C2** `src/MobileEmployeeView.jsx:327` — `<span>{role?.name}</span>` rendered without `isTitled` guard. Coupled to C1; both must land together.

### A11y gaps (L)
- **L1** `src/MobileAdminView.jsx:371` — clickable `<td>` day cells lack `role="button"`, `tabIndex`, keyboard handler, `aria-label`. The neighbor name-cell `<td>` at line 299 already has the full pattern (lines 301-306). The s040 a11y commit `f1934d4` covered ScheduleCell + ColumnHeaderCell + column-header `<th>` + name `<td>`, but missed the day-cell `<td>` in mobile admin.

### Structural observations (J)
- **J1** `src/App.jsx:191,1016,2087,2642` — `published` state is write-only. `useState(false)` + `setPublished(false)` at saveShift + two `setPublished(true)` writers at modal complete callbacks. Zero read sites. Safe to remove all four.
- **J2** `src/views/EmployeeView.jsx:309` — `week2DateStrs` useMemo declared, never read. Safe to remove.

### Security flags (K)
None.

## Non-findings confirmed
- `approvedTimeOffSet` correctly hoisted; no per-cell scans remain in the four grid paths
- `ScheduleCell.handleClick` correctly `useCallback`-wrapped
- All s040 modal focus-trap paths verified Tab-trap + Escape via Playwright smoke
- Native Checkbox refactor preserved visual + adds keyboard semantics
- `cellEvents.some()` calls in mobile cell render operate on per-cell sliced arrays (≤2 items typical), not over `events` map

## Tally
Bucket 1: 0 (deferred to plan) | Bucket 2: 5 | Non-findings: 5

## Audit scope skipped
None.

## Cost
- Stage 0.5 map: built (files=11, hot=2, markers=18)
- Stage 1 static: knip + jscpd both unavailable (npx timeout); LLM covered A/B/G manually
- Stage 2 inventory: ~109k tokens, 5 findings
- Stage 3 triage: synthesized in parent (inventory already structured)
- Total wall-clock: ~5 min
