# [Service/API Name] — Data Contract

## Source
[Where this data comes from — API endpoint, database table, external service, etc.]

## Consumer
[What reads this data — which modules, components, or downstream services.]

## Shape

```
{
  "field_name": "type — description",
  "field_name": "type — description"
}
```

## Constraints
- [Validation rules, required fields, value ranges]
- [Format requirements — e.g., "dates must be ISO 8601"]
- [Rate limits, size limits, auth requirements]

## Example

```json
{

}
```
