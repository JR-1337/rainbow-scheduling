# Handoff — RAINBOW Scheduling

Session 38. `CLAUDE.md` auto-loaded. Read `docs/todo.md`, `docs/decisions.md`, `docs/lessons.md` at session start.

## Session Greeting

S39 extraction steamrolled: App.jsx **8282 → 6400 (−1882)** across S39.3c-h. S33.5 repo hygiene also landed (working tree clean, `dist/` + `Photos/` gitignored, `package-lock.json` committed). All 10 commits pushed to origin. Demo is **2026-04-14 (2 days)**.

Next up: **S39.3i — AdminSettingsModal (~200L) + ChangePasswordModal (~200L) → src/modals/**. Both use exported `Modal` helper, both self-contained. Lands App.jsx near 6000 (plan's stop target). After those 2 the remaining 6 components are large form-heavy + state-coupled (EmployeeView especially, ~840L with 30+ App-scope closures like the deferred S39.4 mobile-admin branch). S40 stop-condition territory.

## State

- Build: PASS (`ee0f25c`)
- Branch: `main`, pushed to origin
- App.jsx: **6400 lines**
- Last commit: `ee0f25c` S38 handoff: write s38 file, prune s33
- Live: https://rainbow-scheduling.vercel.app (Vercel auto-deploying the pushed commits)
- Apps Script live: v2.14 (local v2.15, deploy deferred)

## Hot Files

| Priority | File | Changed? | Why |
|----------|------|----------|-----|
| 1 | `src/App.jsx` | Yes (extensive) | L3311 AdminSettingsModal + L3513 ChangePasswordModal next (grep to confirm lines — will have shifted from 10 deletions) |
| 2 | `src/modals/ShiftEditorModal.jsx` | Yes (new) | Template for Modal-helper-based modal with `../App` deps |
| 3 | `src/modals/RequestTimeOffModal.jsx` | Yes (new) | Template for rolls-own-shell modal (doesn't use Modal helper) |
| 4 | `~/.claude/plans/lovely-launching-marble.md` | No | S39 stop condition: App.jsx < 6000 OR too entangled. ~400L to go. |

## Anti-Patterns (Don't Retry)

- **Deleting an inline component from App.jsx before adding the import** (since S38) — now in lessons.md but fresh enough to keep here one more session. Vite doesn't catch undefined JSX refs at build; fails at runtime only.
- **Executing S39.4 per the plan file** (since S36) — conflicts with decisions.md 2026-02-10. Revisit only after admin state → Context refactor.
- **Listing post-demo deferrals in next-task options** (since S38) — saved as `feedback_no_post_demo_reminders.md`; until 2026-04-14 meeting, drop deferred items from choice menus.

## Blocked

| Item | Blocked By | Notes |
|------|-----------|-------|
| Code.gs v2.15 deploy | JR manual | Harmless but open |
| Email sender upgrade | JR providing sender email | Pre-existing |
| S35 browser verify | JR / Chrome-ext Claude | CLI has no browser tool |

## Key Context

- **Exports added to App.jsx this session** (for extracted files; circular-safe because all uses are inside function bodies per lessons.md): `OFFER_STATUS_COLORS`, `OFFER_STATUS_LABELS`, `SWAP_STATUS_COLORS`, `SWAP_STATUS_LABELS`, `Modal`, `TimePicker`, `GradientButton`, `calculateHours`. Run `grep -n '^export const' src/App.jsx` for the full accumulated set.
- **S39.3i extraction recipe** (mirror S39.3g):
  1. Write extracted file to `src/modals/`
  2. Add import to App.jsx **first**
  3. `awk` delete the inline block (grep current line numbers — they've shifted from prior deletions)
  4. `npm run build` + `npm run preview` + `curl -sI http://localhost:4173/` → 200
  5. Commit with `git add <explicit paths>` (never `-A` or `.`)
- **Remaining inline components after S39.3i** (in App.jsx, sizes approximate): EmployeeFormModal (240L), EmailModal (455L), RequestDaysOffModal (290L), OfferShiftModal (320L), SwapShiftModal (590L), EmployeeView (840L, HIGH RISK — same App-scope closure pattern as deferred S39.4). All are form + state heavy.

## Verify On Start

- [ ] `git status` clean on main
- [ ] `git log --oneline -1` top is `ee0f25c`
- [ ] `git status` — NOT ahead of origin (all pushed at start of session 39)
- [ ] `npm run build` passes
- [ ] `curl -I https://rainbow-scheduling.vercel.app/` → 200
- [ ] `wc -l src/App.jsx` → 6400
- [ ] `AskUserQuestion` to confirm S39.3i (AdminSettings + ChangePassword) — grounding only
