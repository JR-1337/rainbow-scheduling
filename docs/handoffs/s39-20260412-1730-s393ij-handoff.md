# Handoff — RAINBOW Scheduling

Session 39. `CLAUDE.md` auto-loaded. Read `docs/todo.md`, `docs/decisions.md`, `docs/lessons.md` at session start.

## Session Greeting

S39 extraction passed the plan's stop target. App.jsx **6400 → 5824 (−576)** across S39.3i + S39.3j. Demo is **2026-04-14 (2 days)**.

Next up: **S39.3k — RequestDaysOffModal (~290L) → src/modals/**. Smallest remaining inline modal, self-contained (Modal helper + form state + current-user passes). After it the list keeps dropping: OfferShiftModal (~320L), EmailModal (~455L), SwapShiftModal (~590L). EmployeeView (~840L) is last and still HIGH RISK per S38 analysis — same App-scope closure pattern as deferred S39.4. Stop-condition judgement call every step from here on; plan target already met.

## State

- Build: PASS (`610f3be`)
- Branch: `main`, pushed
- App.jsx: **5824 lines**
- Last commit: `610f3be` S39.3j: extract EmployeeFormModal
- Live: https://rainbow-scheduling.vercel.app
- Apps Script live: v2.14 (local v2.15, deploy deferred)

## Hot Files

| Priority | File | Changed? | Why |
|----------|------|----------|-----|
| 1 | `src/App.jsx` | Yes (extensive) | L1035 RequestDaysOffModal next (grep to confirm — will shift after each deletion) |
| 2 | `src/modals/EmployeeFormModal.jsx` | Yes (new) | Template for heavier form-modal extraction with `Input` + `STORE_HOURS` consumers |
| 3 | `src/modals/AdminSettingsModal.jsx` | Yes (new) | Template for tab-pattern modal |
| 4 | `~/.claude/plans/lovely-launching-marble.md` | No | S39 stop target already hit; remaining extractions are judgement-call territory |

## Anti-Patterns (Don't Retry)

- **Listing post-demo deferrals in next-task options** (since S38) — saved as `feedback_no_post_demo_reminders.md`; until 2026-04-14 meeting, drop deferred items from choice menus.

## Blocked

| Item | Blocked By | Notes |
|------|-----------|-------|
| Code.gs v2.15 deploy | JR manual | Harmless but open |
| Email sender upgrade | JR providing sender email | Pre-existing |
| S35 browser verify | JR / Chrome-ext Claude | CLI has no browser tool |

## Key Context

- **New exports this session** (circular-safe, all uses in function bodies): `apiCall`, `Input`, `STORE_HOURS`. Run `grep -n '^export ' src/App.jsx` for the accumulated set.
- **Remaining inline components** (current line numbers in App.jsx):
  - L578 EmailModal (~455L)
  - L1035 RequestDaysOffModal (~290L) ← next
  - L1326 OfferShiftModal (~320L)
  - L1647 SwapShiftModal (~590L)
  - L2236 EmployeeView (~840L, HIGH RISK)
- **Extraction recipe** (unchanged from S38):
  1. Write extracted file to `src/modals/`
  2. Add import to App.jsx **first** (lessons.md entry — Vite doesn't catch undefined JSX at build)
  3. `awk` delete the inline block (grep current lines, they shift)
  4. `npm run build` + `npm run preview` + `curl -sI http://localhost:4173/` → 200
  5. Commit with explicit `git add` paths (never `-A` or `.`)

## Verify On Start

- [ ] `git status` clean on main, not ahead of origin
- [ ] `git log --oneline -1` top is `610f3be`
- [ ] `npm run build` passes
- [ ] `wc -l src/App.jsx` → 5824
- [ ] `curl -I https://rainbow-scheduling.vercel.app/` → 200
- [ ] `AskUserQuestion` to confirm S39.3k (RequestDaysOffModal) vs stop — grounding only
