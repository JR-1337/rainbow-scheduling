# Handoff — RAINBOW Scheduling

Session 35. `CLAUDE.md` auto-loaded. Read `docs/todo.md`, `docs/decisions.md`, `docs/lessons.md`, and the plan file at `~/.claude/plans/lovely-launching-marble.md` at session start.

## Session Greeting

S35 shipped the post-demo security track *early* (at JR's direction): S36 + S37 + S38 are live. JR also chose to skip the S35 browser-verify chunk for now. Demo is **2026-04-14 (2 days away)**.

Next up per plan: either **S35 browser verify** (pre-demo) or jump straight to **S39 opportunistic extractions**. Do NOT re-ask which — ask JR what he wants to land before demo vs. after, then execute. Don't re-audit, don't re-plan.

## State

- Build: PASS (commit `537b050`, Vercel auto-deploys from main)
- Tests: NONE
- Branch: `main`, pushed
- Last commit: `537b050` S36 setup tooling: one-click menu + howto + v2.15 strip
- Live: https://rainbow-scheduling.vercel.app
- Apps Script live = **v2.14** (setup menu still present — JR skipped the v2.15 redeploy; harmless, idempotent). Local `backend/Code.gs` = v2.15. Next paste deploys v2.15 automatically.

## This Session

- S36 (`29bf3b9`): Code.gs HMAC token + SHA-256 pw hash + `verifyAuth(payload)` dual-path (token → callerEmail fallback).
- S37 (`7338259`): new `src/auth.js`, `apiCall` auto-attaches token, AUTH_EXPIRED bounces to login, 34 `callerEmail:` sites stripped.
- S38 (`d4deabc`): `useFocusTrap(dialogRef, isOpen)` wired into `MobileBottomSheet`.
- S36 deploy tooling (`537b050`): one-click `setupAuth` menu shipped + used + stripped locally. Howto at `docs/DEPLOY-S36-AUTH.md`. Login verified working after JR ran setup.

## Hot Files

| Priority | File | Changed? | Why |
|----------|------|----------|-----|
| 1 | `~/.claude/plans/lovely-launching-marble.md` | Yes (added verification gate) | Still the plan; S39/S40 remain. |
| 2 | `src/auth.js` | Yes (new) | Any new auth work touches here. |
| 3 | `src/App.jsx` | Yes | `apiCall` auto-injects token; `loadDataFromBackend`/logouts wired through auth module. |
| 4 | `backend/Code.gs` | Yes (v2.15 local, v2.14 live) | Rotate-secret menu + stripped setup. Any Code.gs edit needs re-deploy. |
| 5 | `src/MobileEmployeeView.jsx` | Yes | `MobileBottomSheet` has focus trap now. |

## Anti-Patterns (Don't Retry)

- **Asking JR "which plan step next?" via AskUserQuestion** (since S35) — the plan file is the answer. Execute in order. JR has now said this 3+ times across sessions. See `feedback_follow_plan_verbatim.md`.
- **Trusting `result.success` from `chunkedBatchSave`** (since S34) — callers must read `data.{totalChunks, failedChunks}` on `success: false`.
- **Top-level use of imported symbols in `src/pdf/generate.js` / `src/email/build.js`** (since S34) — circular imports with `../App` are safe only inside function bodies.

## Blocked

| Item | Blocked By | Notes |
|------|-----------|-------|
| Code.gs v2.15 deploy | JR manual (skipped by choice) | Setup menu still live but harmless. |
| Email sender upgrade | JR providing sender email | Pre-existing. |
| S35 browser verify | JR / Chrome-ext Claude | CLI has no browser tool. |

## Key Context

- **Auth migration state:** `verifyAuth` accepts payload (prefers `token`, falls back to `callerEmail`). Every deployed user still has a plaintext password in the sheet until they log in once — first login migrates them to hash+salt and clears the plaintext column via `changePassword`. Admin `resetPassword` intentionally keeps plaintext (UI shows it) and clears the hash so next login re-migrates.
- **Rotate-secret emergency button:** Sheet menu → Rainbow Admin → Rotate HMAC Secret. Rotating invalidates every session instantly. Documented in `docs/DEPLOY-S36-AUTH.md`.
- **Plan deviation (recorded in `docs/decisions.md`):** JR ordered S36/S37/S38 ahead of S35 browser verify after demo-critical bugs landed. Plan dependency graph still allows S35 before demo; up to JR.
- **Per-chunk verification gate** added to `~/.claude/plans/lovely-launching-marble.md` — mandatory between S36/S37/S38/S39.

## Verify On Start

- [ ] `git status` clean on main, 3 untracked (`Photos/`, `dist/`, `package-lock.json`)
- [ ] `git log --oneline -1` top is `537b050`
- [ ] `npm run build` passes
- [ ] Live site loads: `curl -I https://rainbow-scheduling.vercel.app/` returns 200
- [ ] Grep sweep: `grep -n "callerEmail: currentUser.email" src/App.jsx` returns 0 hits
- [ ] `AskUserQuestion` to confirm pre-demo scope (S35 browser verify) vs post-demo work (S39 extractions) before any Edit/Write — this is grounding, NOT the banned "which plan step?" ask
