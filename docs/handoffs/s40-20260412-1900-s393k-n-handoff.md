# Handoff — RAINBOW Scheduling

Session 40. `CLAUDE.md` auto-loaded. Read `docs/todo.md`, `docs/decisions.md`, `docs/lessons.md` at session start.

## Session Greeting

S39 kept going past the plan's stop target. Four clean modal extractions landed: S39.3k RequestDaysOffModal, S39.3l OfferShiftModal, S39.3m EmailModal, S39.3n SwapShiftModal. App.jsx **6400 → 4597 (−1803 across the full S39 arc, -1227 this batch)**. Demo is **2026-04-14 (2 days)**.

Only one inline extractable remains: **EmployeeView (~840L, HIGH RISK)**. We stopped at SwapShiftModal specifically to put EmployeeView in a fresh context. It uses the same App-scope closure pattern that caused S39.4 (mobile admin) to be deferred — many callbacks/derived data are closed over App's top-level scope. A naive extraction breaks it.

Next up: JR wants **EmployeeView extraction attempted in the new chat**. Read the EMPLOYEE VIEW section (L1009-~1848 in current App.jsx) carefully before planning the extraction — if the closure surface is too big, propose a `currentUser`/callback-prop interface rather than extracting verbatim.

## State

- Build: PASS (`2e7cf4d`)
- Branch: `main`, pushed
- App.jsx: **4597 lines**
- Last commit: `2e7cf4d` S39.3n: extract SwapShiftModal
- Live: https://rainbow-scheduling.vercel.app
- Apps Script live: v2.14 (local v2.15, deploy deferred)

## Hot Files

| Priority | File | Changed? | Why |
|----------|------|----------|-----|
| 1 | `src/App.jsx` | Yes (extensive) | EmployeeView inline @ L1009. EmployeeViewRow @ L963 (already `React.memo`'d, may extract with view). Handoff target. |
| 2 | `src/modals/SwapShiftModal.jsx` | Yes (new, S39.3n) | Template for large step-wizard modal |
| 3 | `src/modals/EmailModal.jsx` | Yes (new, S39.3m) | Uses `Modal` helper + `Checkbox` (newly exported) |
| 4 | `src/modals/OfferShiftModal.jsx` | Yes (new, S39.3l) | Peer to SwapShiftModal, shares pending-check pattern |
| 5 | `src/modals/RequestDaysOffModal.jsx` | Yes (new, S39.3k) | Simplest extraction template |

## Anti-Patterns (Don't Retry)

- **Listing post-demo deferrals in next-task options** (since S38) — until 2026-04-14 meeting, drop deferred items from choice menus.
- **Extracting EmployeeView by pattern-matching the modal recipe** — it has admin-like scope, not modal scope. Modals take all deps as props; EmployeeView reaches into App-level closures in ways modals don't. Read the closure surface before planning.

## Blocked

| Item | Blocked By | Notes |
|------|-----------|-------|
| Code.gs v2.15 deploy | JR manual | Harmless but open |
| Email sender upgrade | JR providing sender email | Pre-existing |
| S35 browser verify | JR / Chrome-ext Claude | CLI has no browser tool |

## Key Context

- **New exports this session** (circular-safe, all uses in function bodies): `Checkbox` (added in S39.3m for EmailModal). Accumulated extraction-era exports at `grep -n '^export ' src/App.jsx`.
- **EmployeeView closure surface to audit before extraction**:
  - Props already: employees, shifts, dates, periodInfo, currentUser, onLogout, timeOffRequests, onCancelRequest, onSubmitRequest, shiftOffers, onSubmit/Cancel/Accept/RejectOffer, shiftSwaps, onSubmit/Cancel/Accept/RejectSwap, periodIndex, onPeriodChange, isEditMode, announcement
  - Suspect closures (read before extracting): any helper functions defined inside the App component body that EmployeeView currently references without prop-passing. grep for function calls inside L1009-L1848 and cross-check against props list.
- **Extraction recipe** (unchanged, proven 4× this session):
  1. Write extracted file to `src/modals/` or `src/views/`
  2. Add import to App.jsx **first** (lessons.md — Vite doesn't catch undefined JSX at build)
  3. `awk 'NR<START || NR>END' src/App.jsx > /tmp/app.jsx && mv /tmp/app.jsx src/App.jsx` (grep bounds fresh each time)
  4. `npm run build` + `npm run preview` + `curl -sI http://localhost:4173/` → 200
  5. Commit with explicit `git add` paths

## Verify On Start

- [ ] `git status` clean on main, not ahead of origin
- [ ] `git log --oneline -1` top is `2e7cf4d`
- [ ] `npm run build` passes
- [ ] `wc -l src/App.jsx` → 4597
- [ ] `curl -I https://rainbow-scheduling.vercel.app/` → 200
- [ ] `grep -n "^const EmployeeView" src/App.jsx` → L1009 (view) and L963 (row)
- [ ] `AskUserQuestion` to confirm: attempt EmployeeView extraction, or stop and pivot to demo polish?
