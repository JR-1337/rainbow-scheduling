# Audit Report — full sweep — 2026-04-29 (s042)

## Verdict
**Needs Attention** — `allViolations` inner-loop redundant work at OTR scale (E1, ~5x reduction available) + PKModal REMOVE date-expand row has no keyboard access on a primary admin surface (L3).

## Health trend (vs. prior audit)
- FIRST_RUN for slug `full` (no prior `audit-*-full.md`). Prior full sweeps shipped under different slugs (s029 schedule-consolidation, s039 audit-deferred). Diff-vs-prior unavailable; future runs will compare against this report.

## Fixed autonomously (this audit)
Bucket 1 — 5 one-line edits, shipped in single commit.

- `[K]` `src/App.jsx:2731` — removed `target="_blank"` from mailto link in admin tooltip (no-op on mailto; removes inconsistency).
- `[J]` `src/App.jsx:60` — dropped `parseLocalDate, escapeHtml` from re-export (zero callers in src/; canonical imports come direct from utils).
- `[J]` `src/constants.js:103` — `export const DESKTOP_SCHEDULE_NAME_COL_PX` → `const DESKTOP_SCHEDULE_NAME_COL_PX` (still used internally on line 104; export was dead per knip).
- `[L]` `src/App.jsx:2194` — added `aria-label` to desktop violations button (was `title=` only; not reliably announced on interactive elements).
- `[L]` `src/App.jsx:1700` — added `aria-label` to mobile admin violations button (mobile + desktop parity).

## Deferred — ranked

### Correctness bugs (I)

**I1** `src/modals/ShiftEditorModal.jsx:113-121` — missing `employee` dep in reset effect; admin opening editor for a different employee on the same date without changing date/shift would see stale defaults. Severity: low (UX, not data-loss). Fix prompt: add `employee?.id` to dep array of useEffect lines 113-121, narrow or remove the eslint-disable. Constraint: do not change seedFor/seedMeetings logic.

**I2** `src/modals/PKModal.jsx:96` — blanket `eslint-disable-line react-hooks/exhaustive-deps` suppresses all missing deps, not just intentional `note` exclusion; hides any future real dep additions. Severity: low (maintainability). Fix prompt: replace blanket disable with inline comment `// note intentionally excluded — one-shot prefill only (guard: !note)`; if eslint still warns, add `note` to deps (the `!note` guard already prevents re-fills).

### Perf — needs measurement (D / E / F)

**E1** `src/App.jsx:717` — `allViolations` useMemo calls `computeWeekHoursFor` inside the inner date loop. **Multiplier**: 35 employees × 5 worked days avg = 175 calls per recompute; each iterates 14 dates → 2,450 date-iterations per shift-state change. Hoisting the call to the outer employee loop reduces to 35 calls × 14 dates = 490 iterations (~5x reduction). Fix prompt: hoist `const wkHours = computeWeekHoursFor(emp.id, dates, shifts, events);` before the inner `for (const date of dates)` loop; reuse already-computed value inside the date loop. Constraint: returned violation array contents must be byte-identical.

### A11y gaps (L)

**L1** `src/modals/PKModal.jsx:478-504` — REMOVE-mode date-expand row is `<div onClick>` with no keyboard or screen-reader semantics; primary admin surface (admins use this to delete PK bookings). Fix prompt: add `role="button"`, `tabIndex={0}`, `aria-expanded={expanded}`, `aria-label`, `onKeyDown(Enter|Space → toggleExpand)`. Constraint: visual unchanged, do not change toggleExpand.

### Structural observations (J)

**J1** `src/App.jsx:67-158` — dead migration-note comment block (~80 lines of "X moved to Y" archaeology referencing symbols already in their final locations for multiple sessions). Fix prompt: delete comment-only lines, preserve `DEFAULT_STAFFING_TARGETS` const at lines 88-92 and re-export comment at line 108.

**J2** `src/App.jsx:673` — `computeWeekHoursFor` is a plain function inside App.jsx (not in `utils/timemath.js` beside `computeDayUnionHours`); observation only.

**J3** `src/App.jsx:1822` — `onCellClick` inline arrow on `MobileAdminScheduleGrid`; no `React.memo` boundary today, so no thrash. Note for the day MobileAdminScheduleGrid is wrapped.

**J4 — jscpd cluster 1**: `src/panels/AdminMyTimeOffPanel.jsx:8` (54L) ↔ `src/panels/MyRequestsPanel.jsx:8`. Extract `RequestListBase` shared component (sortDir state, sortedRequests, formatRequestDates, list render).

**J5 — jscpd cluster 2**: `src/panels/MySwapsPanel.jsx:36` (48L) ↔ `src/panels/ReceivedSwapsHistoryPanel.jsx:51`. Note: ReceivedSwapsHistoryPanel.jsx also appears in J9 — assess together.

**J6 — jscpd cluster 3**: `src/panels/AdminShiftOffersPanel.jsx:82` (47L) ↔ `src/panels/AdminShiftSwapsPanel.jsx:86`. Shared admin-panel list render.

**J7 — jscpd cluster 4**: `src/modals/OfferShiftModal.jsx:185` ↔ `src/modals/SwapShiftModal.jsx:282` (46L) **and** `:236 ↔ :348` (44L). Two overlapping clusters in same file pair — likely collapse into one extraction.

**J8 — jscpd cluster 5**: `src/modals/AdminSettingsModal.jsx:144` (43L) ↔ `src/modals/ChangePasswordModal.jsx:83`. Likely password-form structure.

**J9 — jscpd cluster 6**: `src/panels/ReceivedOffersHistoryPanel.jsx:41` (39L) ↔ `src/panels/ReceivedSwapsHistoryPanel.jsx:46`. Overlaps with J5; assess together.

**J10 — jscpd cluster 0 (known-deferred per s040)**: `src/MobileAdminView.jsx:343` ↔ `src/MobileEmployeeView.jsx:311` (70L). No new entry; tracked in TODO Blocked.

### Security flags (K)

None surviving exclusions. K1 (target=_blank on mailto) shipped in B1.

## Non-findings confirmed

- **I (saveShift stale-closure, App.jsx:1004-1006)**: triage demoted from generalist's I to non-finding. `applyShiftMutation` reads only the functional-prev side; "stale" snapshots are pass-through arguments only, never read on the mutation path. Data loss not possible from this pattern.
- **D (ScheduleCell filter)**: filter runs once per cell over O(1-2) items; not a scan at 35×7 scale.
- **F (App.jsx:1822 inline arrow on MobileAdminScheduleGrid)**: receiver is not React.memo; no perf finding. Retained as J3 observation.
- **L (EmployeeView read-only cells)**: read-only cells require no interactive role.
- **C (unreachable branches)**: 0 findings; s039 sweep already cleared known cases.
- Dead exports `parseLocalDate` / `escapeHtml` / `DESKTOP_SCHEDULE_NAME_COL_PX`: confirmed via grep, moved to B1.

## Tally

Bucket 1: 5 | Bucket 2: 12 | Non-findings: 7

## Audit scope skipped

- `backend/Code.gs` — out of scope for autonomous audit (per skill constraint).
- Customer-facing copy — left untouched per skill constraint.
- `pitchdeck/capture/cover-loop.mjs` — knip flagged unused; out of `src/` scope.
- `playwright` devDependency — knip false positive (used via Playwright MCP).

## Cost

- **Stage 0.5 map**: built fresh (cache cleared on restart). 81 files, 7 hot, 18 marker categories.
- **Stage 1 static**: knip + jscpd both available; 3 dead exports + 101 jscpd clones surfaced.
- **Stage 2 inventory** (Sonnet generalist): ~124k total tokens, 14 files Read.
- **Stage 3 triage** (Sonnet): ~52k total tokens, B1 verified by main session.
- **Stage 4 diff**: FIRST_RUN for slug `full`.
- **Total wall-clock**: ~10 min including session-restart recovery.

## Anti-pattern hits

- **Triage agent misread `DESKTOP_SCHEDULE_NAME_COL_PX` as fully dead** (B1 was "delete line"). Main-session verification caught the internal use at constants.js:104; B1 corrected to drop `export` keyword only.
- One more confirmation that **B1 verification by main session is load-bearing** — the J1 false-positive pattern from s041 repeated here in a different shape.
