# s036 -- 2026-04-28 -- Unified PK modal (create + per-day remove)

## Session Greeting

Read `CONTEXT/TODO.md`, `CONTEXT/DECISIONS.md`, and `CONTEXT/ARCHITECTURE.md`; read `CONTEXT/LESSONS.md` only if preferences may affect approach. Resume from `State` and `Next Step Prompt`. First reply: 1-2 short sentences, a `Pass-forward:` line, and exactly 1 direct question about how to proceed.

Pass-forward: soliton kintsugi -- PK modal shipped + smoked + brand-purple fix shipped; next focus is the multi-session phone-smoke batch (s028-s036) or top Active TODO.

## State

- Project: `/home/johnrichmond007/APPS/RAINBOW Scheduling APP`
- Git: HEAD `0959ce1` on `main`; 2 untracked `.cursor/rules/*.mdc` (pre-existing, not from this session)
- Sibling repo `~/APPS/RAINBOW-PITCH/`: untouched
- Active focus end-of-session: backlog (no specific top item; PK modal redesign was the s035 carry-over and is now shipped)

## This Session

**Commits shipped (2):**

- `bc958dd` -- `feat(schedule): unified PK modal with create + per-day remove`. New `src/modals/PKModal.jsx` (~400 lines), deletes `src/modals/PKEventModal.jsx`, refactors `src/App.jsx` (handler rename, shared mount const, dead-code removal). Plan at `~/.claude/plans/cuddly-prancing-cake.md`.
- `0959ce1` -- `fix(schedule): PK modal uses fixed brand purple #932378, not rotating accent`. Smoke caught `THEME.accent.purple` rotates daily (today landed on green dark `#00863E`). Replaced 3 callsites with local `PK_PURPLE = '#932378'` constant.

**Coding-plan workflow** (`/coding-plan` skill, plan at `~/.claude/plans/cuddly-prancing-cake.md`):

- Phase 1 investigate: enumerated PKEventModal callsites (5: def + import + 2 mounts + 1 comment), read AutofillClearModal as design template.
- Phase 2 (research) skipped: design pattern is internal reference (s035 AutofillClearModal).
- Phase 3 (clarify) used 2-question budget: PK scope (replace), removal granularity (per-day expand), hour-pickers (expose, current).
- Phase 4 predecessor: jiggly-twirling-sparrow.md (s035) explicit template; inherited Decisions + Anti-patterns.
- Phase 5 plan: 6 phases A-F; Opus 4.7 author, Sonnet 4.6 executor + smoker.
- Phase 6 execute (Sonnet 4.6): all 6 phases PASS, no plan-vs-code divergence. Build PASS at every gate. Executor also removed dead `daysWithPKInWeek` (zero callsites after refactor).
- Phase 7 smoke (Sonnet 4.6 + Playwright): 15/16 PASS on prod. 0 console errors. Lone FAIL: CREATE pill rendered green instead of brand purple.

**Modal shipped:**

- New `src/modals/PKModal.jsx`. AdaptiveModal-wrapped. Two modes via 2-segment pill: CREATE (purple bg) + REMOVE (red bg).
- CREATE mode: ports PKEventModal verbatim (date input + 2 TimePickers + Saturday quick-pick chip + note + attendee controls + FT/PT grouped checkbox list with eligibility opacity). `getPKDefaultTimes` day-of-week defaults preserved. Confirm "Schedule for {N}".
- REMOVE mode: per-day-expand over visible week (`activeWeek/week1/week2`). Top rows = each date with >=1 PK booking (chevron + date label + booking count + day-level checkbox). Tap to expand -> child rows (employee + start-end time + per-booking checkbox). Day-level checkbox is derived (all/none/mixed) with native `HTMLInputElement.indeterminate` via callback ref `DayCheckbox` component. `removeBookings` Set keyed by `${empId}|${date}|${startTime}|${endTime}`. Confirm "Remove {N} booking(s)".
- Single mount at App root via shared const referenced from desktop and mobile branch returns (per "Render shared overlays at App root" lesson).

**App.jsx changes:**

- `handleBulkPK` (lines 297-355) renamed to `handlePKConfirm`. New signature accepts `removeBookings` array of tuples `{empId, date, startTime, endTime}` for cross-week multi-slot removal. CREATE branch unchanged (single date+startTime+endTime + addIds). REMOVE branch iterates tuples and filters each `events[`${empId}-${date}`]`.
- Dead `daysWithPKInWeek` (App.jsx:620-634, zero callsites after AutofillClearModal s035 refactor) removed.
- Dual `<PKEventModal>` mounts (App.jsx:2087+2651 in s035 numbering) collapsed to single `{pkModalEl}` shared const.

**Smoke summary** (16-step Playwright on prod, mobile + desktop):

- 15 PASS / 1 FAIL. FAIL was step 3: CREATE pill bg = `rgba(0, 134, 62, 0.19)` (green dark), expected `#932378` (OTR brand purple).
- All functional tests PASS: REMOVE per-day expand + day-level indeterminate + per-booking toggle + CREATE save + REMOVE save + grid pip update + mobile parity.
- 0 console errors throughout. `batchSaveShifts` POST confirmed on each save.

**Brand-purple fix (`0959ce1`):**

- Root cause: `THEME.accent.purple` is `OTR_ACCENT.dark` (line 45 of `src/theme.js`), where `OTR_ACCENT` is the daily rotating index over `OTR.accents[0..4]`. Today the index landed on Green (#00A84D / dark #00863E). Plan author conflated rotating `accent.purple` with the immutable OTR brand purple `#932378`.
- Fix: local `const PK_PURPLE = '#932378';` at top of `src/modals/PKModal.jsx`. Replaced 4 callsites: CREATE pill bg+color (lines 333-334), attendee checkbox fill+border (lines 302-303), Saturday quick-pick chip bg+border+shadow (lines 385-388).
- Original PKEventModal had the same rotating-accent bug; nobody caught it because Saturday chip was rarely the active state.

**Memory writes:**

- `TODO.md`: removed top Active "PK modal redesign" (shipped); added top Completed entry for s036 ship; removed Blocked "JR to delete 22 stale PK on Sat May 9" (REMOVE mode supersedes); added `Anchor: soliton kintsugi` line.
- `DECISIONS.md`: untouched. No durable direction changes.
- `ARCHITECTURE.md`: untouched. No structural change.
- `LESSONS.md`: untouched. The rotating-accent gotcha is derivable from `src/theme.js:9-22` + `:45`; not a graduated-pattern candidate yet. If pattern recurs, consider LESSON entry "do not assume `THEME.accent.*` is fixed-brand; only `THEME.roles.cashier`/`status.*`/`event.*` are fixed".
- Auto-memory: none new.

**Decanting:**

- Working assumptions: `THEME.accent.purple = brand purple #932378` was the false plan-time assumption. Now corrected in code; documented inline in PKModal.jsx header comment for `PK_PURPLE`.
- Near-miss: would have shipped without the smoke catch. Captured in Anti-Patterns.
- Naive next move: spawn `/coding-plan` for whatever is at TODO top without checking that the s036 phone-smoke is also owed. Captured in Anti-Patterns.

**Audit (Step 3):**

- Schema-level: clean. TODO has Active + Blocked + Verification + Completed sections.
- LESSONS: 588 lines, RISK over 200 ceiling carried (no new entries this session, no graduations).
- DECISIONS: 153 lines, under ceiling.
- TODO: ~100 lines, under ceiling.
- ARCHITECTURE: ~170 lines, under ceiling.
- Style soft-warns: pre-existing MD034/MD041 noise carries. None introduced this session.
- Adapter files: untouched.

`Audit: clean (LESSONS 588/200 ceiling carried; pre-existing style soft-warns persist)`

## Hot Files

- `src/modals/PKModal.jsx` (NEW) -- unified PK modal. `onConfirm({ mode, date, startTime, endTime, note, addIds, removeBookings })` is the parent contract. `removeBookings` shape `[{empId, date, startTime, endTime}, ...]` supports cross-week multi-slot removal.
- `src/App.jsx:297` -- `handlePKConfirm` -- rebuilt mutation handler. Preserves `guardedMutation` + `batchSaveShifts` contract. CREATE branch `if (mode === 'create')` gates the add path; REMOVE branch always iterates `removeBookings` regardless of mode.
- `src/theme.js:9-22` + `:45` -- `OTR.accents[]` + rotating `OTR_ACCENT` + `THEME.accent` map. CRITICAL: `THEME.accent.blue/purple/pink` rotate daily; `THEME.roles.*`, `THEME.status.*`, `THEME.event.*` are fixed.
- `src/modals/AutofillClearModal.jsx` -- design template; mirror this for any future segmented-mode-toggle modal.
- `src/utils/employeeSort.js` -- `sortBySarviAdminsFTPT` + `computeDividerIndices`. Reuse for any future employee picker.
- `src/utils/eventDefaults.js` -- `getPKDefaultTimes` (Saturday 10:00-10:45, other days 18:00-20:00).
- `src/utils/violations.js` -- `computeViolations` (s034). Used elsewhere; PK CREATE/REMOVE do NOT run it (PK has no ESA implications).

## Anti-Patterns (Don't Retry)

- Don't reuse `THEME.accent.purple` (or `accent.blue/pink`) as a fixed brand color. They rotate daily. Use `roles.cashier` (#932378) or a literal `#932378` constant.
- Don't ship a feature without smoke even if the plan-vs-code looks clean. The brand-purple bug was only catchable in browser; build PASS does not catch rotating-token mistakes.
- Don't auto-spawn `/coding-plan` next session without checking phone-smoke debt. s028-s036 phone-smoke is still owed by JR.
- Don't bundle PK creation/clearing across the AutofillClearModal. PKModal is its own modal in the same design language; the unified-modal pattern means "one modal per concept, not one modal per app".
- Don't reintroduce `PKEventModal`. Deleted in s036; all logic is in `PKModal.jsx`. If a regression surfaces, fix in PKModal.
- Don't introduce dual modal mounts. Single shared const at App root referenced from both desktop+mobile branch returns (the s035 lesson).
- Don't gate the confirm button on anything but `selected.size > 0` (or `removeBookings.size > 0`). No violation gating, no eligibility gating.
- Don't add a "Remove all in week" preset to PKModal REMOVE mode. JR didn't ask, week is short, per-day rows are compact.
- Don't strip `note` field from REMOVE mode child rows. Plan calls for it; smoker observed empty-note bookings; verify on bookings WITH notes that they render.

## Blocked

- iPad print preview side-by-side -- since 2026-04-26
- 089adaa N meetings + 0d3220e sick-day-event-wipe / title-clear smokes -- pending JR phone-test -- since 2026-04-25
- s028+s029+s030+s031+s032+s033+s034+s035+s036 commits with deferred phone-smoke -- carried (s036 was Playwright-smoked on prod 15/16; phone-smoke still owed)
- Employee-view hours-lockdown live verification -- testguy is inactive in Sheet; verified by code inspection only
- S62 2-tab settings split + retroactive-default fix -- since 2026-04-14
- CF Worker SWR cache -- since 2026-04-14
- Consecutive-days 6+ warning -- since 2026-04-14
- Payroll aggregator path 1 -- since 2026-04-12
- Amy ADP rounding rule discovery -- since 2026-04-26
- S39.4 mobile admin extraction -- blocked by admin state -> context provider refactor
- Natalie Sirkin Week 18 manual re-entry -- since s034 smoke (2026-04-28)
- s034 backend live smoke -- deferred to email-format session per JR

## Key Context

- The PKModal mirrors AutofillClearModal's design language (segmented mode toggle + checkbox list + prominent confirm). New 2-mode-toggle features should follow the same pattern.
- `PK_PURPLE = '#932378'` is currently a local constant in `src/modals/PKModal.jsx`. If a 2nd module needs the same fixed brand purple, promote to `THEME.brand.purple` (does not exist yet).
- `removeBookings` tuple shape is the new general PK removal contract. Today's `handlePKConfirm` always honors it regardless of mode -- a CREATE call CAN also pass `removeBookings` (CREATE+remove-this-slot is how edit-mode worked in PKEventModal and still works here via `removeIds` mapped to tuples in PKModal's CREATE confirm path).
- Untracked `.cursor/rules/blast-radius.mdc` + `.cursor/rules/ui-ux-design.mdc` are NOT from this session. Surface to JR; do not auto-stage.
- The plan at `~/.claude/plans/cuddly-prancing-cake.md` has the full execution context including the 16-step Verification Summary used by the Playwright smoker.

## Verify On Start

1. Read `CONTEXT/TODO.md` -- Anchor is `soliton kintsugi`. No top Active item; pick from backlog or check phone-smoke debt.
2. `git log --oneline -3` should show `0959ce1`, `bc958dd`, then the s036 handoff commit.
3. `git status -s` should show 2 untracked `.cursor/rules/*.mdc` (pre-existing) and nothing else after this handoff is committed.
4. Before any backend work: re-read `CONTEXT/ARCHITECTURE.md` `Deploy topology` -- Apps Script lives in `otr.scheduler@gmail.com`'s Drive standalone; access via `script.google.com` while that account is the active session.
5. Adapter files: not touched s036. Skip unless harness-switch context arises.

## Next Step Prompt

Per HANDOFF check order:

- (a) Shipped-but-unverified: s036 unified PK modal -- Playwright PASS 15/16 on prod, brand-purple fix shipped post-smoke. Phone-smoke owed but rolled into the carried multi-session phone-smoke batch (s028-s036). No standalone smoke gate this session.
- (b) External gates: phone-smoke for s028-s036 carried (JR-owned). Apps Script live smoke still deferred to email-format session.
- (c) Top active TODO: no clear top item; backlog pickup.

(c) defaults to JR's choice. Most natural opener: ask JR which backlog item to take next -- candidates include EmailModal v2 + email-format pass (long-deferred), in-app bug fixes from `docs/audit-2026-04-27-deferred.md`, or the Sarvi-confirmed phrasing sweep across pitch artifacts. JR-driven pick.

If switching harnesses, read shared CONTEXT first; AGENTS.md is canonical -- shims rarely need repair.
