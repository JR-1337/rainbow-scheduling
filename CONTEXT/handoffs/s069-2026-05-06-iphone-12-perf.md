# s069 -- 2026-05-06 -- iPhone 12 perf cumulative + autofill holiday skip

scumble. instanton.

## Session Greeting

Read `CONTEXT/TODO.md` first; the user's first prompt drives any further `CONTEXT/*` reads on demand. Resume from `State` and `Next Step Prompt`. First reply: 1-2 short sentences, a `Pass-forward:` line, and exactly 1 direct question about how to proceed.

Pass-forward: 8 commits of iPhone 12 perf shipped (A1-A6 + B2 + C3 + holiday + long-press scroll); awaiting Sarvi natural-use feedback before chasing B1.

## State

- **Project:** RAINBOW Scheduling APP at `~/APPS/RAINBOW Scheduling APP/`. No cross-repo touches this session.
- **Git:** `main` at `0703fbf`, 8 perf commits + 2 TODO commits pushed since prior handoff `s068`. Step 7 commits + pushes this handoff on top.
- **Active focus end-of-session:** iPhone 12 perf work cumulative shipped end-to-end. Sarvi confirmed scroll works post `62d992c`. Save-time freeze fixes are in but not separately confirmed by Sarvi (her natural-use feedback is the verification signal).
- **Working assumption (durable for follow-up only):** iPhone 12 sits in WebKit's 400-450 MB JS-heap pressure band (vs ~1 GB+ on iPhone 15+). The "freeze sometimes" symptom shape maps to Jetsam Strict-tier eviction-and-recompile of compiled JS, not a code bug. Diagnosis was symptom-driven, not Web Inspector-traced; verification is observational ("she can scroll now").

## This Session

**Shipped-but-unverified:**
- A1+A2+A3 (`c386cb9`), A4 (`c83c1fe`), A5+B2+C3 (`c7c3406`), holiday-skip (`137c909`), long-press scroll fix (`62d992c`), A6 mobile grid + cell memoization (`ffeafd6`). Sarvi confirmed scroll. Save-time fixes not separately confirmed.
- Save-time mechanisms addressed: prop-identity stabilization (week arrays useMemo'd, EMPTY_EVENTS frozen, `today` hoisted), `AnimatedNumber` rAF chain dropped on hot paths, `useDeferredValue` on shifts/events for `allViolations` recompute, mobile grids and cells now memoized.
- `docs/iphone12-freeze-fixes-2026-05-06.md` is the ranked candidate list and forward reference.

**External ops:** none.

**Audit:** clean (11 LESSONS atomicity soft-warns, 2 new from this session's entries with compound `; ` Rule clauses; no relocations).

**Memory writes:** `CONTEXT/TODO.md` (anchor swap to `scumble. instanton.`, iPhone 12 entry rolled up to cumulative-shipped, holiday-autofill moved Active -> Completed, A6 added to Completed, oldest Completed trimmed). `CONTEXT/LESSONS.md` +2 entries (non-passive touchmove iOS scroll cliff; useDeferredValue on inputs not outputs); `lessons_pre=25 lessons_post=27`, no cadence trigger.

**Prune:** Anti-Patterns: 1 dropped (s064 ship-over-patch completionism, 5+ behind, did not recur), 0 graduated, 4 kept, 1 net-new s069. Hot Files: 2 dropped (s068 PDF chunk-load -- closed end-to-end with Sarvi smoke; s066 tier authority stack -- prod-smoked verified), 0 graduated, 2 kept (s068 schedule sort, CONTEXT/DECISIONS re-hot), 4 added s069.

## Hot Files

- `src/MobileAdminView.jsx` + `src/MobileEmployeeView.jsx` -- mobile grids now have extracted memoized `MobileAdminScheduleCell` + `MobileScheduleCell` (per A6). Future cell behavior changes thread through the cell component prop API, not inline JSX. (origin: s069)
- `src/components/EmployeeRow.jsx` + `src/components/ColumnHeaderCell.jsx` -- desktop perf hot path; A4 dropped `AnimatedNumber` here. Modal `AnimatedNumber` preserved at `ShiftEditorModal.jsx:687,693,703`. (origin: s069)
- `src/App.jsx` -- perf cluster: `useDeferredValue` on shifts/events at line ~700; mobile-admin `useCallback`'d handlers near line ~2000 (`handleMobileAdminCellClick`, `handleMobileNameClick`, `handleColumnHeaderClick` reuse); `getStaffingTarget` `useCallback` at line 717. (origin: s069)
- `src/hooks/useLongPress.js` -- simplified after non-passive listener removal (`62d992c`). If scroll regresses on iPhone 12, this is the suspect. (origin: s069)
- `src/utils/employeeSort.js` + `src/constants.js` -- 4-bucket schedule sort + name-rank constant. If a new admin's first name isn't in `SCHEDULE_ROW_FIRST_NAME_ORDER` they alpha-tail. (origin: s068)
- `CONTEXT/DECISIONS.md` -- 22.6KB, archive cycle expected when next entry crosses 25k chars. (re-hot s064)

## Anti-Patterns (Don't Retry)

- **Don't add `useCallback` wraps to inline handlers passed into a child unless the child is already wrapped in `React.memo`.** Premature optimization: the desktop branch was already fully `useCallback`'d when audited this session, and mobile inline handlers are no-op until a memo wrapper exists on the receiving component. Verify the consumer's memo state before chasing handler stability. (origin: s069)
- **Don't dispatch a research subagent when the user-reported symptom matches a known frontend pattern.** Sarvi's "long-ass error that disappears" was the Vite chunk-load family; the agent's run found "no code bug" because the bug class was environmental (stale chunk hash). (origin: s068)
- **Don't spawn ad-hoc triage subagents when an installed skill already defines the workflow.** (origin: s065)
- **Don't fix tier persistence by Sheet `isOwner` edits alone while `App.jsx` still strips `isAdmin`/`adminTier` for every non-owner.** (origin: s066)
- **Don't treat optimistic UI as proof of persistence** before `saveEmployee` payload still carries tier fields for admin1 callers. (origin: s066)

## Blocked

- See `CONTEXT/TODO.md` ## Blocked.

## Key Context

- **iPhone 12 perf cumulative state:** 8 commits since `s068` shipped to address Sarvi's "freeze sometimes" + scroll-freeze symptoms. Scroll-freeze root cause was 490 non-passive `touchmove` listeners (one per `LongPressCell`); fix removed them, Sarvi confirmed scroll works. Save-time freeze fixes shipped but not separately confirmed.
- **Mobile grid memoization architecture:** mobile mirrors desktop -- `MobileAdminScheduleGrid` / `MobileScheduleGrid` wrapped in `React.memo`; `MobileAdminScheduleCell` / `MobileScheduleCell` extracted as memoized per-cell components. Per-cell derived computation and long-press payload construction live inside the cells. Pre-flight stabilized parent-side handlers in App.jsx + EmployeeView.jsx.
- **Autofill skips stat holidays now (`137c909`).** `autoPopulateWeek` returns early on `isStatHoliday(date)` after the existing `unavailable`-event guard. Manual shift creation on holidays still allowed; clear-week unchanged. 10 stat dates listed in `src/utils/storeHours.js`.
- **`useDeferredValue` is on inputs, not outputs:** `deferredShifts` + `deferredEvents` route into `allViolations` useMemo deps so urgent renders skip the ~6,860-lookup recompute. Violations badge briefly lags after a Save then catches up.
- **Apps Script live = v2.32.5** (paste-deployed s066). No backend writes this session.
- **Vite vendor split:** main bundle now 95 kB gzip app + 45 kB cached React vendor (B2 in `c7c3406`); 500 kB chunk warning gone.

## Verify On Start

1. `CONTEXT/TODO.md` -- anchor `scumble. instanton.`; iPhone 12 freeze entry top of Active.
2. `git log --oneline -10` -- expect this run's handoff commit on top, then `0703fbf`, `ffeafd6`, `62d992c`, `c7c3406`, `137c909`, `1c1b0f4`, `c83c1fe`, `c386cb9`.
3. `git status -s` -- clean.
4. If JR reports any continued mobile slowness from Sarvi: B1 (lazy-load 14 modals, ~190 kB raw out of main bundle) is the next candidate; A6 + B2 already addressed parse-time and render-time costs.

## Next Step Prompt

Default falls (a) -> (b) -> (c):

- (a) **Sarvi cumulative iPhone 12 smoke (gating, observational).** Cumulative perf work shipped end-to-end. Sarvi confirmed scroll works (`62d992c`); Save-time freeze symptom not separately confirmed. Wait for natural-use feedback. **Caution: if the freeze-on-Save report doesn't recur over Sarvi's normal use this week, the cumulative work cured it -- do NOT chase B1 (lazy-load modals) preemptively.** B1 has a real cold-load tradeoff (~200-400 ms first-open per modal on 4G); only worth it if Sarvi flags continued slowness AND the cause is parse-time, not render-time.
- (b) **External gates:** none other than (a).
- (c) **Top active TODO:** holiday-autofill behavior is unverified by Sarvi/JR (next stat holiday is 2026-05-18 Victoria Day); routine schedule work over the next two weeks will surface it naturally. No action needed unless reported.

If switching harnesses, read shared `CONTEXT/` first; `AGENTS.md` is canonical -- shims rarely need repair.
