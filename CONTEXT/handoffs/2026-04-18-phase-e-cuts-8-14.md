<!-- SCHEMA: handoff
Purpose: session continuity for the next chat. Ephemeral.
Write mode: one file at a time. Prior handoff deleted when new one is written.
Rules: filename YYYY-MM-DD-{short-slug}.md; required sections per HANDOFF_PROMPT.md; do not restate adapter content; ASCII operators only.
-->

# Handoff -- 2026-04-18 -- Phase E sub-area 4: 7 more App.jsx cuts (8-14) shipped

## Session Greeting

This session shipped 7 more App.jsx extraction cuts via the same ship-merge-verify rhythm: cuts 8 LoginScreen, 9 ColumnHeaderEditor, 10 TooltipButton, 11 ScheduleCell + getAvailabilityShading, 12 dead REQUEST_STATUS / OFFER_STATUS enums, 13 getStoreHoursForDate + override-refs relocation, 14 EmployeeRow. App.jsx 3702 -> 3228 lines (-474, -13% this session; -919, -22% across both sessions). Bundle byte-identical 464.79 kB through cut 12, +1 byte to 464.80 kB after cut 13's re-export. Read in this order: `CONTEXT/TODO.md`, `CONTEXT/DECISIONS.md` (top entry is this session), `CONTEXT/ARCHITECTURE.md`, this file.

First reply: 1-2 short sentences, `Pass-forward:` with only essential carryover, exactly 1 direct question about how to proceed. Default next step is cracking open the main `App()` body -- standalone top-of-file extracts are exhausted.

## State

- Project root: `/home/johnrichmond007/APPS/RAINBOW Scheduling APP/`
- Branch: main, HEAD `6c2f562` == origin/main (0 ahead, 0 behind before this handoff commit)
- Working tree: handoff ceremony writes only
- Prod: LIVE at https://rainbow-scheduling.vercel.app on a post-cut-14 bundle (Vercel auto-rebuild after each push). Cuts 8 + 9 + 11 are pure component extracts; cut 10 is appended to existing primitives module; cut 12 is dead-code removal; cut 13 is mechanical relocation of mutable-ref module (no behavior change); cut 14 is component extract enabled by cut 13. Functional risk is low across all 7 cuts. NO live smoke this session by JR -- next chat should ask whether smoke is needed before continuing.
- Apps Script: v2.23.0 LIVE (unchanged from prior session). Hash-only auth working.
- Build: `npm run build` PASS at HEAD (~464.80 kB)
- App.jsx: 3228 lines (was 3702 at session start, -474 / -13%)
- Audit plan: `~/.claude/plans/adversarial-audit-fix-plan.md` -- standalone top-of-file extraction surface fully exhausted. Sub-area 6 (ref-elimination Context refactor) still parked but now isolated to `src/utils/storeHoursOverrides.js`.

## This Session

1. **Boot read**: read CONTEXT/TODO.md, CONTEXT/DECISIONS.md (top entry), prior handoff. Ran Verify-On-Start (git status clean, log matches, build PASS, App.jsx 3702, all expected new modules present). Confirmed default next step (LoginScreen) with JR via AskUserQuestion. JR picked LoginScreen.
2. **Cut 8 (`4c15ca3`) -- LoginScreen**: extracted to `src/components/LoginScreen.jsx`. Deps: React useState, THEME, apiCall, setAuthToken, setCachedUser, ChangePasswordModal -- all importable. Single call site in App.jsx unchanged. App.jsx 3702 -> 3581 (-121). Bundle byte-identical.
3. **Cut 9 (`286d5fe`) -- ColumnHeaderEditor**: extracted to `src/components/ColumnHeaderEditor.jsx`. Pure component -- storeHoursOverrides + staffingTargetOverrides arrive via props (not closure), so it decoupled cleanly from the parked sub-area-6 Context refactor. Two call sites in App.jsx unchanged. App.jsx 3581 -> 3460 (-121). Bundle byte-identical.
4. **Cut 10 (`e4ffb33`) -- TooltipButton**: appended to existing `src/components/primitives.jsx`. primitives.jsx imports widened to useState + useEffect. App.jsx imports TooltipButton back. App.jsx 3460 -> 3420 (-40). Bundle byte-identical.
5. **Cut 11 (`d109608`) -- ScheduleCell + getAvailabilityShading**: extracted to `src/components/ScheduleCell.jsx`. getAvailabilityShading folded in as private helper (only call site was inside ScheduleCell). EmployeeRow stays in App.jsx for this cut (still uses parked getStoreHoursForDate). App.jsx 3420 -> 3307 (-113). Bundle byte-identical. Used `sed -i '150,256d'` after Edit-tool churn proved fragile on the long JSX block.
6. **Mid-session decision -- flag dead code**: REQUEST_STATUS + OFFER_STATUS enums had zero references anywhere in src/ (status color/label maps already lived in src/constants.js). Per global "No Silent Removal" rule, flagged before deleting; JR said "delete and continue".
7. **Cut 12 (`08f455d`) -- drop dead REQUEST_STATUS / OFFER_STATUS enums**: same pattern as cut 2's SWAP_STATUS removal. App.jsx 3307 -> 3282 (-25). Bundle byte-identical.
8. **Cut 13 (`90fc5c4`) -- relocate getStoreHoursForDate + override refs**: created `src/utils/storeHoursOverrides.js` with module-level refs + setter functions (setStoreHoursOverrides, setStaffingTargetOverrides, getStaffingTargetOverrides, getStoreHoursForDate). App.jsx imports them with aliases (syncStoreHoursOverrides / syncStaffingTargetOverrides) to avoid collision with the local useState setter. useEffects now call setters instead of mutating module vars from outside the module. Re-exported `getStoreHoursForDate` from App.jsx so the 6 external import sites (EmployeeView, MobileAdminView, MobileEmployeeView, ShiftEditorModal x2, App's own line 161 EmployeeRow ref) keep working without churn. **Mechanical relocation only** -- the parked sub-area-6 Context refactor still applies on top; this cut just isolates the smell to one file. App.jsx 3282 -> 3274 (-8). Bundle 464.79 -> 464.80 kB (+1 byte for re-export).
9. **Cut 14 (`6c2f562`) -- EmployeeRow**: extracted to `src/components/EmployeeRow.jsx`. Now possible because cut 13 broke the `getStoreHoursForDate` dependency on App.jsx's module file. EmployeeRow imports getStoreHoursForDate from the new storeHoursOverrides module directly. ScheduleCell import in App.jsx removed (only EmployeeRow consumes it). App.jsx 3274 -> 3228 (-46). Bundle 464.80 kB unchanged.
10. **CONTEXT syncs (this handoff ceremony)**: TODO.md gained 1 new Completed entry; Active line updated. DECISIONS.md gained 1 new entry at top. ARCHITECTURE.md updated: App.jsx line count, src/components surface enumerated, src/utils enumerated, parked-ref module noted.
11. **Audit**: skipped during code-only commits; performed at handoff ceremony.

## Hot Files

| Priority | File | Why |
|----------|------|-----|
| 1 | `src/App.jsx` | 3228 lines. Standalone top-of-file extracts exhausted. The remaining ~3000 lines is the main `App()` component body itself: schedule grid + admin drawer + state + effects + handlers. Cracking it open requires prop/state-threading decisions per section -- multi-session work. |
| 2 | `src/utils/storeHoursOverrides.js` | New home for the parked module-level mutable refs. Sub-area-6 Context refactor will REPLACE this file with a Provider; do not extend it. |
| 3 | `src/components/{LoginScreen,ColumnHeaderEditor,ScheduleCell,EmployeeRow}.jsx` | New surface this session. Future schedule-grid cuts should follow the same naming + the same pattern (pure-only, props for everything, no closures). |
| 4 | `~/.claude/plans/adversarial-audit-fix-plan.md` | Sub-area 4 standalone phase done. Sub-area 6 (Context refactor for storeHoursOverrides) still parked. |
| 5 | `CONTEXT/TODO.md` | Top Active: Sarvi-batch + Phase A+B+C save-failure smoke (still blocked on Sarvi Monday 2026-04-19 -- TOMORROW). |

## Anti-Patterns (Don't Retry)

- Do NOT use the Edit tool on long JSX blocks with subtle whitespace differences. Cut 11's ScheduleCell extract had three Edit-tool failures (whitespace mismatch, modified-since-read, and an awkward placeholder rename) before falling back to `sed -i '150,256d'`. For block deletions where you control both ends, sed line-range delete is faster and surer.
- Do NOT thread getStoreHoursForDate through App.jsx as the source for child modules. Importing it from `src/utils/storeHoursOverrides.js` directly is correct (the App.jsx re-export is a backward-compat bridge for legacy importers, not a pattern to extend).
- Do NOT name the new sync functions setStoreHoursOverrides / setStaffingTargetOverrides as imports in App.jsx -- they collide with the local useState setter. Alias them to syncStoreHoursOverrides / syncStaffingTargetOverrides.
- Do NOT keep stale imports after extraction. After cut 14, ScheduleCell was no longer used directly in App.jsx; the import had to be removed in the same cut.
- All prior-session anti-patterns still apply: ship-merge-verify per cut, do NOT trust Vite's build pass alone for runtime undefined-references, plan-time knowledge does not survive to execution time.

## Blocked

See `CONTEXT/TODO.md#Blocked`. Top-of-mind:

- Sarvi-batch + Phase A+B+C save-failure smoke -- waiting on Sarvi Monday 2026-04-19 (TOMORROW)
- Payroll aggregator -- waiting on Sarvi Counterpoint / ADP discovery
- Sub-area 6 (Context refactor for `_storeHoursOverrides` + `_staffingTargetOverrides`) -- own dedicated branch; smell is now isolated to `src/utils/storeHoursOverrides.js`

## Key Context

- App.jsx is now mostly the main `App()` component (lines ~235-3228). Top-of-file standalone components are all gone. The remaining cuts must address sections of `App()` itself: state initialization, useEffects, handlers, the JSX tree (admin schedule grid, drawer, modals wiring). Each subsection needs careful prop-threading.
- Ship-merge-verify rhythm continued: commit -> push to main -> Vercel auto-rebuild -> next cut. JR did NOT live-smoke individual cuts this session; functional risk is low (component extracts + dead-code removal + mechanical relocation), but worth asking before next cut whether to smoke first.
- The `getStoreHoursForDate` re-export from App.jsx (line 41) is intentionally there to avoid churning 6 external importers. It is a compatibility bridge, not a pattern. Sub-area-6 Context refactor will eventually remove the storeHoursOverrides module entirely and update all importers in one shot.
- The rename pattern `setStoreHoursOverrides as syncStoreHoursOverrides` is necessary because of the useState setter collision -- if you extend the override module with more state, follow the same alias convention.
- Apps Script v2.23 still LIVE (no backend changes this session).
- Sarvi smoke is tomorrow (Monday 2026-04-19) -- prep should already be done; nothing to add.
- Global rules from prior session still in force: complete-the-operation, refresher-checkpoints, plan-time-knowledge.

## Verify On Start

- `git status` -- expect clean (handoff write may be in flight)
- `git log --oneline -12` -- top should be handoff commit, then `6c2f562`, `90fc5c4`, `08f455d`, `d109608`, `e4ffb33`, `286d5fe`, `4c15ca3`, `91c0f4b` (prior handoff), `eba776f`, `654c1ae`, `21c5f62`
- `git rev-list --left-right --count origin/main...HEAD` -- `0 0` confirms synced
- `npm run build` -- should PASS (~464.80 kB)
- `wc -l src/App.jsx` -- expect 3228 lines (down from 3702 at session start, 4147 at start of prior session)
- `ls src/components` -- new files this session: LoginScreen.jsx, ColumnHeaderEditor.jsx, ScheduleCell.jsx, EmployeeRow.jsx (plus prior session's primitives, uiKit, CollapsibleSection, Button, AdaptiveModal)
- `ls src/utils/storeHoursOverrides.js` -- expect file exists
- `grep -n "getStoreHoursForDate" src/App.jsx` -- expect line 41 re-export + line ~14 import (no body definition; it lives in storeHoursOverrides.js)
- `curl -s https://rainbow-scheduling.vercel.app/ | grep -oE 'index-[A-Za-z0-9_-]+\.js'` -- some post-cut-14 bundle hash
- Confirm with JR whether anything in the live UI looks off after the 7 cuts (none were explicitly live-smoked this session -- low functional risk but JR's call)

## Next Step Prompt

Default: pause for live smoke before continuing.

The standalone top-of-file extraction surface is fully exhausted. Remaining App.jsx work means cracking open the `App()` component body -- qualitatively different work that requires prop/state-threading decisions per section, likely a multi-session effort. Before starting that, JR should:

1. Live-smoke at least the schedule grid + login flow on the post-cut-14 bundle (cuts 8 + 11 + 14 touched the most user-visible surface).
2. Decide between continuing Phase E into the App() body OR pivoting to other Active items: Welcome email on new-employee create, Schedule-change notifications to Sarvi, sub-area-6 Context refactor (closes audit item 6 + retires storeHoursOverrides.js), or holding for tomorrow's Sarvi smoke.

If JR wants to continue Phase E into the App() body, suggested first targets (in order of independence from grid state):
- The auth/onLogin flow setup (~30 lines around the currentUser useState)
- The keyboard-shortcut + auth-failure-callback useEffects
- The save-button cycle (SAVE/GO LIVE/EDIT) state machine if it's a coherent block
- LAST: the schedule-grid JSX itself (most entangled with admin state)

If JR opens a new topic instead, follow him.
